# --- Secrets ---
.env
*.key

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- ML artifacts (these live on the Hugging Face Hub, not git) ---
# Model weights / adapters
*.safetensors
*.bin
*.gguf
checkpoints/
outputs/
adapters/
merged/

# Full training data (only data/golden/, data/samples/, data/seed/ are committed)
data/generated/
data/raw/
data/train.jsonl
data/dev.jsonl
data/test.jsonl

# Full harvested corpus is reproducible via `python -m data.harvest_cocoa`.
# Commit only the sample + NOTICE (CDLA-Sharing lets us share, but keep the repo lean).
data/harvested/*.jsonl
!data/harvested/*.sample.jsonl

# Experiment tracking / caches
wandb/
.cache/
hf_cache/
*.log

# Evaluation artifacts are committed for submission evidence.

# Local Vercel project link (deployment metadata is account-specific)
.vercel/

# Merged/balanced training set (derived; contains BSD/Tatoeba data — do not commit)
data/train_full.jsonl
