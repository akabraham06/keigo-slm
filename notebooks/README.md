# Notebooks

- `train_qlora.ipynb` — _TODO_: the Unsloth QLoRA run, intended for Colab / RunPod (one
  A100/H100; Qwen3 ≤1.7B fits a 24GB card). Mirrors `src/train/sft.py`.

Keep heavy exploration here, but the reproducible pipeline lives in `src/` + the `Makefile`.
