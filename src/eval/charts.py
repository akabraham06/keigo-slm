"""The 'money slide': the register diagonal.  [STUB]

Scatter/heat of source_band (x) vs predicted output band (y). Perfect preservation hugs the
diagonal y=x; a flattening model shows up as a flat horizontal band stuck at neutral.
Renders 'flattening' as a shape anyone can read. See the dataviz guidance before styling.

TODO: matplotlib scatter with jitter, one panel per model, save results/diagonal.png.
"""
from __future__ import annotations


def main() -> None:
    raise NotImplementedError("Plot source band vs predicted band per model; save to results/.")


if __name__ == "__main__":
    main()
